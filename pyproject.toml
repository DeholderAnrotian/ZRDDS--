[build-system]
requires = ["setuptools>=42", "wheel","pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "zrpy"
version = "0.1.0"
description = "Python bindings for ZRDDS"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
