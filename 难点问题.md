1. 函数式绑定（Function Binding）
尝试方案:
最初我们对 C++ 函数采用 pybind11 的函数式绑定，即将单独的函数直接暴露给 Python。
遇到的问题
对于简单函数效果不错，但当函数参数类型复杂（如引用、指针、模板类型）时，经常出现绑定失败或者 Python 调用出错。
返回值管理不明确时，容易出现悬空引用或者性能开销大。
当函数存在重载或默认参数时，需要大量额外声明，导致维护成本增加。
结论
函数式绑定适合小型、独立函数，但无法应对项目中复杂对象和类方法的绑定需求。
因此，需要更系统的方法来处理类和对象的绑定。
2. 复杂类关系绑定（Complex Class Binding）
尝试方案:
将 C++ 类及其继承关系、组合关系直接绑定到 Python，包括虚函数、静态成员和枚举。
遇到的问题:
基类指针返回派生类对象时，Python 无法正确识别派生类类型。
对象生命周期管理复杂，多对象持有同一子对象时容易悬空或内存泄漏。
循环依赖和类间复杂关系导致绑定顺序敏感，稍有改动就可能引发绑定错误。
Python 调用接口仍然不够直观，需要理解 C++ 内部结构才能使用。
结论:
直接绑定复杂类虽然解决了函数式绑定无法处理对象关系的问题，但依然维护成本高、易出错，且对 Python 用户不友好。
因此，需要通过增加一层封装，将复杂性隐藏，并提供 Python 端稳定直观的接口。
3. 封装层绑定（Wrapper Layer Binding）
尝试方案:
在 C++ 端增加封装层，对复杂类进行二次封装：
对内部对象管理生命周期
简化接口:
提供统一错误处理
Python 端只与封装层交互，而不直接操作底层复杂类。
效果:
封装层隐藏了复杂的类关系和生命周期管理，让 Python 使用更直观、安全。
即使内部 C++ 类结构发生变化，Python 接口也能保持稳定。
降低了维护成本，绑定逻辑更清晰、可扩展。

函数式绑定：简单函数可行，但复杂对象无法处理 → 出现问题
复杂类关系绑定：处理了对象关系，但维护困难且 Python 不直观 → 出现问题
封装层绑定：隐藏复杂性，提供稳定接口 → 最终解决方案