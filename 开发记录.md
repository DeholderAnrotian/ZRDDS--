### 9/3 完全由中间层接管的封装结构
- 由封装类对象管理原生类对象
- 代码中有任何中文字符(包括字符串中)有可能编译成功但是报紫色的python无法导入的错误
- 记得加#pragma once
- 工厂管理的子实体还是需要暴露指针
- Aliases 引用和起别名
- 头文件中禁止#include "Aliases"否则会造成循环依赖 (include cycle)
- 常量枚举对照传参
- LongLongTypeSupport:#include "ZRBuiltinTypesTypeSupport.h"\
- 非工厂模式创建对象内部指针务必在构造函数初始化


- 自定义数据类型支持问题
- 封装对象持有裸指针问题
- 各个类功能函数补全